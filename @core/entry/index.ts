import buildSsr from 'vite-ssr/build';
import { defineConfig, mergeConfig } from 'vite';
import mainConfig from './vite.config';

export { buildSsr };

export { defineConfig, mergeConfig, mainConfig };
